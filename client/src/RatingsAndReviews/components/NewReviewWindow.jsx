import React from 'react';

export default function NewReviewWindow({Visibility}){

  if (Visibility === 'show') {

    return (
      <div class="NewReviewWindow" >
        <h1>hello</h1>
      </div>
    );
  }
};